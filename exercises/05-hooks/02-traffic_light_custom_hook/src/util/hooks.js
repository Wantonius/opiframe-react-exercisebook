import {useState,useEffect} from 'react';

export const useLight = (initialState = "red") => {
	
	//TODO: using the existing hooks from the previous exercise create a custom hook that returns the color state to Trafficlight.

}